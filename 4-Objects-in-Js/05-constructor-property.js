let x = {}; // When we write code like this so javaScript thinks that this code is like this => let x = new Object().

// We also have a lot's of bultin objects
// Every Object has constructor property 
new String(); // '',"", ``
new Boolean(); // true, false
new Number(); // 1,2,3...